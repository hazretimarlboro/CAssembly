PUSH 10;
PUSH 0;
PUSH 999999999;

POP;
POP;
POP;
POP;            // STACK UNDERFLOW TEST

PUSH 5;
PUSH 0;
DIV rax;        // DIVISION BY ZERO TEST

PUSH 5;
PUSH 0;
MOD rbx;        // MOD BY ZERO TEST

MOV rax 123;
MOV rbx rax;
MOV rcx rbx;
MOV rdx rcx;

MOV invalid 5;  // INVALID REGISTER TEST

ADD rax;
PUSH 7;
ADD rax;

MUL rbx;
PUSH 3;
MUL rbx;

MVN rax;

OR rax;

LOG rax;
LOG rbx;
LOG rcx;
LOG rdx;

HLT;

PUSH 999;       // SHOULD NEVER EXECUTE